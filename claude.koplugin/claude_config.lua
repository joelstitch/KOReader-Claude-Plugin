-- AI Assistant Plugin – Configuration File
-- -------------------------------------------------------
-- Pre-set your API keys here, OR use the in-reader menu:
-- Menu → AI Assistant → Configure API Key
-- (The menu edits the key of the currently selected provider.)
--
-- The "YOUR_API_KEY_HERE" placeholders are ignored by the plugin,
-- so leave the lines you don't use untouched.
--
-- Keys:
--   Anthropic: https://console.anthropic.com
--   DeepSeek:  https://platform.deepseek.com
--   MiniMax:   https://platform.minimax.io
--              (CN users: https://platform.minimaxi.com – keys are region-bound)
-- -------------------------------------------------------

return {
    api_key      = "YOUR_API_KEY_HERE",   -- ← Anthropic Claude key
    deepseek_key = "YOUR_API_KEY_HERE",   -- ← DeepSeek key
    minimax_key  = "YOUR_API_KEY_HERE",   -- ← MiniMax key
}
